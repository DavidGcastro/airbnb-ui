import { Dimensions } from 'react-native';

const styles = {
  wrapper: {
    width: '100%',
    flex: 1,
    justifyContent: 'center',
    position: 'absolute',
    alignItems: 'center',
    paddingTop: 40,
    paddingLeft: 10,
    paddingRight: 10,
    backgroundColor: 'white'
  }
};

export default styles;
